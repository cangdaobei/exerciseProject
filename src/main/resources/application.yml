server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///test?user=root&password=admin&useUnicode=true&characterEncoding=UTF8&serverTimezone=GMT
    username: root
    password: admin
  aop:
    #false:开启jdk动态代理，true:使用cglib动态代理
    proxy-target-class: false